
# fugu_scope.py (GPU client)
fastplotlib
pygfx
glfw
# imgui-bundle 1.92 reworked the font/texture API (dropped get_tex_data_as_rgba32,
# added BackendFlags_.renderer_has_textures). wgpu>=0.29's imgui backend expects 1.92;
# older imgui-bundle (e.g. 1.6.x) breaks it. Pin both together so neither drifts.
imgui-bundle>=1.92
wgpu>=0.29
numpy
scipy
zeroconf
pyyaml

# scope-client.py (legacy matplotlib reference)
matplotlib
pandas
